server:
  port: 7070

#logging:
#  level:
#    org.springframework: DEBUG
#    com.furkan: INFO
#  file:
#    name: auth-service.log
#  logback:
#    rollingpolicy:
#      max-file-size: 20MB
spring:
  security:
    user:
      name: furkan
      password: 123
  zipkin:
    enabled: true
    base-url: http://localhost:9411
    service:
      name: auth-service

  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: password
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5500/AuthDB
    username: postgres
    password: asd123asd
  jpa:
    hibernate:
      ddl-auto: update

TokenKey: ${TOKEN_KEY}